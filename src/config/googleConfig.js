// import Constants from 'expo-constants';

// // Read EXPO_GOOGLE_CLIENT_ID from app config extras (recommended) or fall
// // back to environment variables. This lets you set the secret at build-time
// // via app.json/app.config.js extras or via process env when running locally.
// const extras = (Constants.expoConfig && Constants.expoConfig.extra) || (Constants.manifest && Constants.manifest.extra) || {};

// export const EXPO_GOOGLE_CLIENT_ID = extras.EXPO_GOOGLE_CLIENT_ID || process.env.EXPO_GOOGLE_CLIENT_ID || '';

// export default {
//   EXPO_GOOGLE_CLIENT_ID,
// };
